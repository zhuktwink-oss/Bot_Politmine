version: '3.8'
services:
  bot:
    build: .
    container_name: my_telegram_bot
    restart: unless-stopped
    volumes:
      - bot_data:/app/data
    environment:
      - PORT=8080

volumes:
  bot_data: